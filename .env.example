# Google Gemini API key — used by scripts/generate-brand-images.ts (Nano Banana 2).
# Get one at https://aistudio.google.com/apikey. Never commit your real value.
GOOGLE_API_KEY=

# Other vars live here too if needed (Supabase, etc.). Keep .env.local untracked.
